/** An Integer tester created by Flik Enterprises. */
public class Flik {

    /** Am includeing the problem line below in comments
     * public static boolean isSameNumber(Integer a, Integer b) {
     *
     * The issue was in using Integer classes instead of int primitive types
     *
     * @param a
     * @param b
     * @return
     */
    public static boolean isSameNumber(int a, int b) {
        return a == b;
    }
}
